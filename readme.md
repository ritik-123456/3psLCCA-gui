# Dev Quickstart

### Setup (first time)
```bash
pip install -r requirements.txt
```

### Run
```bash
python -m gui.main
```

### Project root
Run all commands from the project root — the folder containing `gui/`.

```
3psLCCA/
├── gui/
│   └── main.py
└── requirements.txt
```